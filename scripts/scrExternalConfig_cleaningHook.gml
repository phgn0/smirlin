/// scrExternalConfig_cleaningHook()
/// clean some values from external_config
// NOT USED CURRENTLY
// buildings

/*var buildings_config = global.config[? "buildings"];
var keys = buildings_config[? "directory_keys"];

for (var i = 0; i < ds_list_size(keys); i++) {
    var building = buildings_config[? keys[| i]];
    
    
}


        // replace object name string with object index
        var obj_id = asset_get_index(file_map[? "gm_obj"]);
        
        if (is_undefined(obj_id)) {
            // invalid object name -> discard object
        } else {
            file_map[? "gm_obj"] = obj_id;
        }*/
