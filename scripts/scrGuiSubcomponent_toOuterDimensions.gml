/// scrGuiSubcomponent_toOuterDimensions(subcomponent, inner_width, inner_height)
/// Return the outer width/ height equivalent to the given inner width/ height.

return array_of(argument0[? "outer_width"] * argument1, 
        argument0[? "outer_height"] * argument2);
