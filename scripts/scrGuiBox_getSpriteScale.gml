/// scrGuiBox_getSpriteScale(sprite, width, height)
/// Returns the draw scaling neccessary so that the sprite will be 
///   drawn to the provided dimensions.

return array_of(
    argument1 / sprite_get_width(argument0),
    argument2 / sprite_get_width(argument0));
